# Swift Package Manager build artifacts
.build/
dist/

# Xcode, if you later open Package.swift there
.swiftpm/
*.xcodeproj
xcuserdata/

# Regenerated by Scripts/generate-icons.sh - not source of truth
BrandAssets/AppIcon.iconset/

.DS_Store

# Internal-only working notes for Claude Code — never pushed to the
# public repo (see README.md instead, which is the real public-facing doc).
CLAUDE.md
